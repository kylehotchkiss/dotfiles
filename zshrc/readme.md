# `.zshrc` 

## Getting Started

1) Clone Repo
`git clone . .dotfiles`

2) Switch to `.dotfiles` `.zshrc`
`rm ~/.zshrc && ln -s ~/.dotfiles/zshrc/.zshrc ~/.zshrc`

3) Use the new `.zshrc`
`source ~/.zshrc`